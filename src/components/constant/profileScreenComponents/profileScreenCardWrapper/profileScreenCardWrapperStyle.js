import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  cardView: {
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: '#fff',
    marginTop: 20,
    padding: 20,
    borderRadius: 10,
  },
});
