import { StyleSheet } from "react-native";

export default StyleSheet.create({
    buttonStyle: {
        backgroundColor: '#f06d06',
        borderRadius:7
    },
    buttonText: {
        color: '#fff',
        fontWeight: '600',
    }
})